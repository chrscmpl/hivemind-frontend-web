import { IEnvironment } from './environment.interface';

export const environment: IEnvironment = {
  origin: 'http://localhost:3000',
  api: 'http://localhost',
  cachingStrategy: 'aggressive',
  animateIdeaEntry: true,
};
