import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class IdeaPaginationService {
  // private requestsMap = new Map<string, PaginatedRequestManager<>>();

  constructor() {}
}
