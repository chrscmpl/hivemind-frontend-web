import { KeepYDirective } from '../directives/keep-y.directive';

describe('KeepYDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepYDirective();
    expect(directive).toBeTruthy();
  });
});
