import { FocusOnEntryDirective } from '../directives/focus-on-entry.directive';

describe('FocusOnEntryDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnEntryDirective();
    expect(directive).toBeTruthy();
  });
});
