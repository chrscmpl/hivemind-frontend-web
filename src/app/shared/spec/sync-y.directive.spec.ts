import { SyncYDirective } from './sync-y.directive';

describe('SyncYDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncYDirective();
    expect(directive).toBeTruthy();
  });
});
