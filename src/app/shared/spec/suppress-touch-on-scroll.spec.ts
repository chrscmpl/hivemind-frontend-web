import { SuppressTouchOnScrollDirective } from '../directives/suppress-touch-on-scroll.directive';

describe('SuppressTouchOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SuppressTouchOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
