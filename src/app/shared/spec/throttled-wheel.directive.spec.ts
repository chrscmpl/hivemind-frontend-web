import { ThrottledWheelDirective } from '../directives/throttled-wheel.directive';

describe('ThrottledWheelDirective', () => {
  it('should create an instance', () => {
    const directive = new ThrottledWheelDirective();
    expect(directive).toBeTruthy();
  });
});
