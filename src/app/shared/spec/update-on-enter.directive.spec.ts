import { UpdateOnEnterDirective } from '../directives/update-on-enter.directive';

describe('UpdateOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
